import { renderSafeMarkdown } from './web-remote-markdown'

const MARKDOWN_SOURCE = renderSafeMarkdown.toString()

export function renderWebRemoteManifest(): string {
  return JSON.stringify({ name: 'Proma', short_name: 'Proma', start_url: '/app/', scope: '/', display: 'standalone', theme_color: '#2563eb', background_color: '#f4f6fb', icons: [{ src: '/icon-192.svg', sizes: '192x192', type: 'image/svg+xml', purpose: 'any maskable' }, { src: '/icon-512.svg', sizes: '512x512', type: 'image/svg+xml', purpose: 'any maskable' }] })
}

export function renderWebRemoteIcon(): string {
  return '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128"><rect width="128" height="128" rx="28" fill="#2563eb"/><path d="M34 36h60v12H34zm0 22h60v12H34zm0 22h38v12H34z" fill="white"/></svg>'
}

export function renderWebRemoteStatic(nonce: string): string {
  return `<!doctype html>
<html lang="zh-CN"><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#2563eb"><meta name="apple-mobile-web-app-capable" content="yes"><link rel="manifest" href="/manifest.webmanifest"><link rel="icon" href="/icon.svg"><link rel="apple-touch-icon" href="/icon-192.svg"><meta name="apple-mobile-web-app-title" content="Proma"><title>Proma 远程</title>
<style nonce="${nonce}">
:root{color-scheme:light dark;font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;font-size:16px;--bg:#f4f6fb;--surface:#fff;--surface2:#eef1f7;--text:#172033;--muted:#68738a;--line:#d9dfeb;--blue:#2563eb;--blue2:#dbeafe;--green:#16805a;--amber:#b45309;--red:#c73535;--shadow:0 6px 22px #18264212}*{box-sizing:border-box}*{-webkit-tap-highlight-color:transparent}body{margin:0;min-height:100vh;background:var(--bg);color:var(--text);padding:calc(10px + env(safe-area-inset-top)) max(12px,env(safe-area-inset-right)) calc(18px + env(safe-area-inset-bottom)) max(12px,env(safe-area-inset-left))}button,input,textarea{font:inherit;font-size:16px}button{border:0;border-radius:12px;padding:11px 15px;background:var(--blue);color:#fff;font-weight:650;min-height:44px}button:disabled{opacity:.55}input,textarea{width:100%;border:1px solid var(--line);border-radius:12px;padding:12px;background:var(--surface);color:var(--text);outline:none}textarea{resize:none;min-height:48px;max-height:150px}.shell{max-width:900px;margin:0 auto}.card{background:var(--surface);border:1px solid var(--line);border-radius:18px;box-shadow:var(--shadow)}.hidden{display:none!important}.muted{color:var(--muted)}.topbar{position:sticky;top:0;z-index:4;background:color-mix(in srgb,var(--bg) 92%,transparent);backdrop-filter:blur(12px);display:flex;gap:10px;align-items:center;padding:8px 0 12px}.topbar h1,.topbar h2{margin:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.topbar h1{font-size:20px}.topbar h2{font-size:18px}.top-copy{min-width:0;flex:1}.subtitle{font-size:13px;color:var(--muted);margin-top:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.icon-button{padding:8px 11px;background:var(--surface);color:var(--text);border:1px solid var(--line);min-width:44px}.status{display:inline-flex;align-items:center;gap:6px;border-radius:999px;padding:6px 10px;font-size:13px;font-weight:700;white-space:nowrap;background:var(--surface2);color:var(--muted)}.status.running{color:var(--green);background:#dff7ec}.status.connecting{color:var(--amber);background:#fff3d6}.status.disconnected{color:var(--red);background:#ffe2e2}.status.running:before{content:"";width:7px;height:7px;border-radius:50%;background:currentColor;animation:pulse 1.1s infinite}@keyframes pulse{50%{opacity:.3;transform:scale(.65)}}.toolbar{display:flex;gap:8px;align-items:center;margin:6px 0 12px}.select{width:auto;min-height:42px;padding:8px 10px}.session-card{display:flex;align-items:center;gap:12px;padding:15px;margin:10px 0;cursor:pointer}.session-main{min-width:0;flex:1}.session-title{font-weight:750;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.session-meta{font-size:13px;color:var(--muted);margin-top:6px;display:flex;gap:8px;flex-wrap:wrap}.running-pill,.pending-pill{border-radius:999px;padding:3px 8px;font-size:12px;font-weight:750}.running-pill{color:var(--green);background:#dff7ec}.pending-pill{color:var(--amber);background:#fff3d6}.session-time{font-size:12px;color:var(--muted);white-space:nowrap}.messages{display:flex;flex-direction:column;gap:10px;padding:8px 0 18px;min-height:42vh}.bubble{max-width:92%;padding:12px 14px;border-radius:18px;line-height:1.55;overflow-wrap:anywhere}.bubble.user{align-self:flex-end;background:var(--blue);color:#fff;border-bottom-right-radius:5px}.bubble.assistant,.bubble.system,.bubble.tool{align-self:flex-start;background:var(--surface);border:1px solid var(--line);border-bottom-left-radius:5px;box-shadow:var(--shadow)}.bubble.system{width:100%;max-width:100%;text-align:center;border:0;box-shadow:none;background:transparent;color:var(--muted);font-size:13px}.bubble p{margin:0 0 8px}.bubble p:last-child{margin-bottom:0}.bubble pre,.tool-detail pre{white-space:pre-wrap;overflow:auto;background:var(--surface2);border-radius:10px;padding:10px;font:13px ui-monospace,SFMono-Regular,Menlo,monospace}.bubble code{background:var(--surface2);border-radius:5px;padding:1px 4px;font:14px ui-monospace,SFMono-Regular,Menlo,monospace}.bubble a{color:inherit;text-decoration:underline}.tool-card{width:100%;max-width:100%;padding:0;overflow:hidden}.tool-card summary{list-style:none;cursor:pointer;padding:13px 14px;display:flex;justify-content:space-between;gap:8px}.tool-card summary::-webkit-details-marker{display:none}.tool-card summary:after{content:"展开";font-size:12px;color:var(--muted)}.tool-card[open] summary:after{content:"收起"}.tool-detail{padding:0 14px 14px}.tool-status{font-size:12px;color:var(--muted);margin-left:auto;white-space:nowrap}.tool-status.done{color:var(--green)}.tool-status.error{color:var(--red)}.thinking{margin:0 0 8px;color:var(--muted);font-size:14px}.thinking summary{cursor:pointer}.aborted{color:var(--muted);font-size:14px}.cursor{display:inline-block;width:7px;height:1.1em;background:currentColor;vertical-align:-.15em;animation:blink .9s infinite}@keyframes blink{50%{opacity:0}}.back-bottom{position:fixed;bottom:calc(92px + env(safe-area-inset-bottom));left:50%;transform:translateX(-50%);z-index:3;background:var(--text);color:var(--surface);border-radius:999px;padding:10px 16px}.interaction-stack{position:sticky;bottom:76px;z-index:3}.approval{border:2px solid #e4a72c;background:#fff8df;padding:14px;margin:8px 0;box-shadow:var(--shadow)}.approval h3{margin:0 0 7px;font-size:16px}.approval p{margin:5px 0;color:#5d4b20}.approval pre{white-space:pre-wrap;max-height:130px;overflow:auto;background:#fff0bd;padding:9px;border-radius:9px;font:13px ui-monospace,SFMono-Regular,Menlo,monospace}.approval-actions{display:flex;gap:10px}.approval-actions button{flex:1}.approval-actions .deny{background:#9b3d3d}.toast{position:fixed;top:calc(10px + env(safe-area-inset-top));left:50%;transform:translateX(-50%);z-index:20;background:var(--text);color:var(--surface);padding:11px 15px;border-radius:12px;box-shadow:var(--shadow);max-width:calc(100vw - 32px)}.composer{position:sticky;bottom:0;background:color-mix(in srgb,var(--bg) 94%,transparent);backdrop-filter:blur(12px);padding:8px 0 calc(4px + env(safe-area-inset-bottom));display:flex;gap:8px;align-items:flex-end}.composer textarea{flex:1}.composer-actions{display:flex;gap:7px}.stop{background:var(--red)}.pair-card{max-width:430px;margin:15vh auto;padding:22px}.pair-card h1{margin-top:0}.pair-card .stack{display:grid;gap:10px}.pair-error{color:var(--red);min-height:22px;margin:8px 0 0}@media(prefers-color-scheme:dark){:root{--bg:#101522;--surface:#182131;--surface2:#222d3e;--text:#edf2fb;--muted:#9ba8bd;--line:#344055;--blue2:#1d355a;--shadow:0 8px 28px #0005}.status.running{background:#153c32}.pending-pill{background:#4a3818;color:#ffd27a}.approval{background:#433715;color:#ffe7a3;border-color:#b98b27}.approval p{color:#ffe7a3}.approval pre{background:#594a20}.bubble.user{color:#fff}}
</style></head><body><div class="shell">
<section id="pair" class="pair-card card"><h1>连接 Proma 远程</h1><p class="muted">输入桌面端生成的 6 位配对码，即可在手机上查看和操作会话。</p><div class="stack"><input id="code" inputmode="numeric" autocomplete="one-time-code" maxlength="6" placeholder="6 位配对码"><input id="label" autocomplete="device-name" placeholder="设备名称"><button id="pairBtn" type="button">开始配对</button></div><p id="pairError" class="pair-error"></p></section>
<section id="app" class="hidden"><header id="listTop" class="topbar"><div class="top-copy"><h1>Proma 远程</h1><div class="subtitle">选择一个会话继续工作</div></div><span id="listStatus" class="status">空闲</span><button id="refresh" class="icon-button" type="button" aria-label="刷新">↻</button></header><div id="listToolbar" class="toolbar hidden"><select id="workspaceFilter" class="select" aria-label="工作区筛选"></select></div><main id="sessions"></main><section id="detail" class="hidden"><header class="topbar"><button id="back" class="icon-button" type="button" aria-label="返回">‹</button><div class="top-copy"><h2 id="title">会话</h2><div id="workspaceTitle" class="subtitle"></div></div><span id="detailStatus" class="status">空闲</span></header><div id="toast" class="toast hidden"></div><div id="interactions" class="interaction-stack"></div><div id="messages" class="messages"></div><button id="bottomButton" class="back-bottom hidden" type="button">回到底部</button><div class="composer"><textarea id="input" rows="1" placeholder="输入消息"></textarea><div class="composer-actions"><button id="stop" class="stop hidden" type="button">中止</button><button id="send" type="button" disabled>发送</button></div></div></section></section></div>
<script nonce="${nonce}">
const renderSafeMarkdown=${MARKDOWN_SOURCE};
const $=id=>document.getElementById(id);const allowedRoles=new Set(['user','assistant','tool','system','unknown']);let sessions=[],workspaces=new Map(),workspaceFilter='all',current=null,ws=null,reconnectTimer=null,reconnectDelay=1000,streamNode=null,streamText='',manualClose=false,connectionState='idle',pendingPermissionIds=new Set(),localPermissionIds=new Set(),nearBottom=true,toolNodes=new Map();
function showPair(){manualClose=true;if(ws){ws.close();ws=null}current=null;$('pair').classList.remove('hidden');$('app').classList.add('hidden');prefillDeviceName()}
function showApp(){$('pair').classList.add('hidden');$('app').classList.remove('hidden')}
function prefillDeviceName(){if($('label').value)return;const ua=navigator.userAgent;$('label').value=/Android/i.test(ua)?'Android 手机':/iPhone|iPad/i.test(ua)?'iPhone':'浏览器设备'}
function roleClass(role){return allowedRoles.has(role)?role:'unknown'}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function toast(text){const node=$('toast');node.textContent=text;node.classList.remove('hidden');clearTimeout(toast.timer);toast.timer=setTimeout(()=>node.classList.add('hidden'),2800)}
function setConnection(state){connectionState=state;const labels={connected:'已连接',connecting:'连接中',disconnected:'已断开',idle:'空闲'};for(const id of ['listStatus','detailStatus']){const node=$(id);if(node){node.textContent=state==='connected'&&current?.running?'运行中':labels[state];node.className='status '+(state==='connected'&&current?.running?'running':state)}}}
function setRunning(running){if(current)current.running=running;setConnection(running?'connected':connectionState==='connected'?'connected':'idle');$('send').textContent=running?'插入':'发送';$('stop').classList.toggle('hidden',!running)}
function relativeTime(value){const diff=Math.max(0,Date.now()-Number(value||0));if(diff<60000)return'刚刚';if(diff<3600000)return Math.floor(diff/60000)+' 分钟前';const date=new Date(value);const today=new Date();if(date.toDateString()===today.toDateString())return'today '+date.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}).replace(' ','');return String(date.getMonth()+1).padStart(2,'0')+'-'+String(date.getDate()).padStart(2,'0')}
async function dataText(data){if(typeof data==='string')return data;if(data instanceof Blob)return data.text();if(data instanceof ArrayBuffer)return new TextDecoder().decode(data);if(ArrayBuffer.isView(data))return new TextDecoder().decode(data);return String(data)}
async function api(path,options={}){const response=await fetch(path,{...options,credentials:'include',headers:{'Content-Type':'application/json',...(options.headers||{})}});if(response.status===401){showPair()}if(!response.ok){const error=new Error((await response.json().catch(()=>({}))).error||response.status);error.status=response.status;throw error}return response.status===204?null:response.json()}
function renderSessions(){let visible=sessions.filter(s=>workspaceFilter==='all'||s.workspaceId===workspaceFilter).sort((a,b)=>b.updatedAt-a.updatedAt);$('sessions').innerHTML=visible.map(s=>{const name=workspaces.get(s.workspaceId)||'未分组';return '<article class="session-card card" data-session-id="'+esc(s.id)+'"><div class="session-main"><div class="session-title">'+esc(s.title)+'</div><div class="session-meta"><span>'+esc(name)+'</span>'+(s.running?'<span class="running-pill">运行中</span>':'')+(s.hasPendingPermission?'<span class="pending-pill">待审批</span>':'')+'</div></div><time class="session-time">'+relativeTime(s.updatedAt)+'</time></article>'}).join('')||'<div class="card" style="padding:18px;text-align:center;color:var(--muted)">没有可显示的会话</div>';document.querySelectorAll('[data-session-id]').forEach(node=>node.addEventListener('click',()=>openSession(node.dataset.sessionId)))}
function renderWorkspaceFilter(){const entries=[...workspaces.entries()];$('listToolbar').classList.toggle('hidden',entries.length<2);$('workspaceFilter').innerHTML='<option value="all">所有工作区</option>'+entries.map(([id,name])=>'<option value="'+esc(id)+'">'+esc(name)+'</option>').join('');$('workspaceFilter').value=workspaceFilter}
async function load(){workspaces=new Map((await api('/api/workspaces')).map(w=>[w.id,w.name]));sessions=await api('/api/sessions');renderWorkspaceFilter();renderSessions();if(current){const latest=sessions.find(s=>s.id===current.id);if(latest){current=latest;setRunning(latest.running)}}}
function statusLabel(status){return status==='generating'?'进行中':status==='emitted'?'已发出调用':status==='done'?'完成':'失败'}
function toolLabel(name,input){let summary=String(input||'');try{const parsed=JSON.parse(summary);summary=String(parsed.command??parsed.file_path??parsed.path??summary)}catch{}summary=summary.slice(0,60);if(name==='Bash')return'执行命令 · '+summary;if(name==='Read')return'读取文件 · '+summary;if(name==='Write')return'写入文件 · '+summary;if(name==='Edit')return'编辑文件 · '+summary;if(name.startsWith('mcp__planning__'))return'规划：'+name.slice('mcp__planning__'.length);if(name==='EgoBrowser')return'操作浏览器';return name}
function blockHtml(block){if(block.kind==='text')return renderSafeMarkdown(block.text);if(block.kind==='thinking')return'<details class="thinking"><summary>思考过程</summary>'+renderSafeMarkdown(block.text)+'</details>';if(block.kind==='aborted')return'<div class="aborted">已中止</div>';if(block.kind==='run_summary')return'<div class="bubble system">'+(block.success?'本轮完成':'本轮失败')+(block.durationMs!=null?' · '+(block.durationMs/1000).toFixed(1)+' 秒':'')+'</div>';if(block.kind==='tool_result')return'<div class="tool-detail"><pre>'+esc(block.resultPreview)+'</pre></div>';if(block.kind==='tool_call'){const input=block.inputPreview||'';return '<details class="tool-card bubble"><summary><span>'+esc(toolLabel(block.toolName,input))+'</span><span class="tool-status '+(block.status==='done'?'done':block.status==='error'?'error':'')+'">'+statusLabel(block.status)+'</span></summary><div class="tool-detail"><pre>'+esc(input)+'</pre>'+(block.resultPreview?'<pre>'+esc(block.resultPreview)+'</pre>':'')+'</div></details>'}return''}
function messageHtml(message){const role=roleClass(message.role);const blocks=(message.blocks||[]).map(blockHtml).join('');return '<article class="bubble '+role+'">'+blocks+'</article>'}
function renderMessages(messages){$('messages').innerHTML=messages.map(messageHtml).join('');$('messages').scrollTop=$('messages').scrollHeight;clearStream();toolNodes.clear()}
async function refreshHistory(){if(!current)return;renderMessages(await api('/api/sessions/'+encodeURIComponent(current.id)+'/messages?limit=200'))}
function clearStream(){if(streamNode)streamNode.remove();streamNode=null;streamText=''}
function appendStream(text){if(!streamNode){streamNode=document.createElement('article');streamNode.className='bubble assistant';streamNode.innerHTML='<div></div>';$('messages').appendChild(streamNode)}streamText+=text;streamNode.firstElementChild.innerHTML=renderSafeMarkdown(streamText)+'<span class="cursor"></span>';if(nearBottom)$('messages').scrollTop=$('messages').scrollHeight;else $('bottomButton').classList.remove('hidden')}
function addToolStatus(event){let node=event.toolUseId?toolNodes.get(event.toolUseId):null;if(!node){node=document.createElement('div');node.className='tool-line';$('messages').appendChild(node);if(event.toolUseId)toolNodes.set(event.toolUseId,node)}node.textContent=toolLabel(event.toolName,event.input)+' · '+statusLabel(event.status);if(nearBottom)$('messages').scrollTop=$('messages').scrollHeight}
function upsertPermission(request){if(pendingPermissionIds.has(request.requestId)||document.querySelector('[data-permission-id="'+CSS.escape(request.requestId)+'"]'))return;pendingPermissionIds.add(request.requestId);const card=document.createElement('article');card.className='approval card';card.dataset.permissionId=request.requestId;card.innerHTML='<h3>需要审批：'+esc(request.toolName)+'</h3><p>'+esc(request.description)+'</p><pre>'+esc(request.toolInput||request.command||'')+'</pre><div class="approval-actions"><button class="deny" type="button" data-behavior="deny">拒绝</button><button type="button" data-behavior="allow">允许</button></div>';card.querySelectorAll('[data-behavior]').forEach(button=>button.addEventListener('click',async()=>{button.disabled=true;card.querySelectorAll('button').forEach(b=>b.disabled=true);card.querySelector('h3').textContent='处理中…';localPermissionIds.add(request.requestId);try{await api('/api/permissions/'+encodeURIComponent(request.requestId),{method:'POST',body:JSON.stringify({behavior:button.dataset.behavior})})}catch(error){card.querySelectorAll('button').forEach(b=>b.disabled=false);toast(error.message)}}));$('interactions').appendChild(card)}
function removePermission(id){const card=document.querySelector('[data-permission-id="'+CSS.escape(id)+'"]');if(card&&!localPermissionIds.has(id))toast('已在桌面处理');card?.remove();pendingPermissionIds.delete(id);localPermissionIds.delete(id)}
async function handleEvent(event){if(event.type==='ready')return;if(event.type==='run_started'){setRunning(true);return}if(event.type==='text_delta'){appendStream(event.text);return}if(event.type==='tool_status'){addToolStatus(event);return}if(event.type==='permission_request'){upsertPermission(event.request);return}if(event.type==='permission_resolved'){removePermission(event.requestId);return}if(event.type==='interaction_unavailable'){toast(event.message||'请在桌面处理');return}if(event.type==='sdk_message'){clearStream();if(event.message)$('messages').insertAdjacentHTML('beforeend',messageHtml(event.message));if(nearBottom)$('messages').scrollTop=$('messages').scrollHeight;return}if(event.type==='refresh_required'){await refreshHistory();return}if(event.type==='title_updated'){if(current){current.title=event.title;$('title').textContent=event.title}return}if(event.type==='run_completed'||event.type==='run_stopped'){setRunning(false);await Promise.all([refreshHistory(),load()])}}
function scheduleReconnect(){if(manualClose||!current||reconnectTimer)return;reconnectTimer=setTimeout(()=>{reconnectTimer=null;connect()},reconnectDelay);reconnectDelay=Math.min(30000,reconnectDelay*2)}
function connect(){if(!current)return;const old=ws;ws=null;if(old)old.close();manualClose=false;setConnection('connecting');const socket=new WebSocket(location.origin.replace(/^http/,'ws')+'/api/stream');ws=socket;socket.addEventListener('open',()=>{reconnectDelay=1000;setConnection('connected');socket.send(JSON.stringify({type:'subscribe',sessionIds:[current.id]}))});socket.addEventListener('message',async event=>{try{void handleEvent(JSON.parse(await dataText(event.data)))}catch(error){toast('实时事件解析失败')}});socket.addEventListener('close',event=>{if(ws!==socket)return;ws=null;if(event.code===1008){showPair();return}setConnection('disconnected');scheduleReconnect()});socket.addEventListener('error',()=>setConnection('disconnected'))}
async function openSession(id){current=sessions.find(s=>s.id===id);if(!current)return;$('title').textContent=current.title;$('workspaceTitle').textContent=workspaces.get(current.workspaceId)||'未分组';$('listTop').classList.add('hidden');$('listToolbar').classList.add('hidden');$('sessions').classList.add('hidden');$('detail').classList.remove('hidden');$('interactions').innerHTML='';pendingPermissionIds.clear();await refreshHistory();connect()}
$('pairBtn').addEventListener('click',async()=>{const button=$('pairBtn');if(button.disabled)return;button.disabled=true;try{await api('/api/pair',{method:'POST',body:JSON.stringify({code:$('code').value,label:$('label').value})});showApp();await load()}catch(error){if(error.status===401){const existing=await fetch('/api/sessions',{credentials:'include'}).catch(()=>null);if(existing?.ok){showApp();await load();return}}$('pairError').textContent=error.message}finally{button.disabled=false}});$('workspaceFilter').addEventListener('change',event=>{workspaceFilter=event.target.value;renderSessions()});$('refresh').addEventListener('click',()=>{void load()});$('back').addEventListener('click',()=>{manualClose=true;const old=ws;ws=null;if(old)old.close();current=null;$('detail').classList.add('hidden');$('listTop').classList.remove('hidden');renderWorkspaceFilter();renderSessions()});$('stop').addEventListener('click',()=>{if(current)void api('/api/sessions/'+encodeURIComponent(current.id)+'/stop',{method:'POST'}).catch(error=>toast(error.message))});$('send').addEventListener('click',async()=>{if(!current)return;const value=$('input').value.trim();if(!value)return;$('input').value='';try{await api('/api/sessions/'+encodeURIComponent(current.id)+'/send',{method:'POST',body:JSON.stringify({message:value})});setRunning(Boolean(current.running))}catch(error){toast(error.message)}});$('input').addEventListener('input',event=>{const node=event.target;node.style.height='auto';node.style.height=Math.min(node.scrollHeight,150)+'px';$('send').disabled=!node.value.trim()});$('messages').addEventListener('scroll',()=>{nearBottom=$('messages').scrollHeight-$('messages').scrollTop-$('messages').clientHeight<80;if(nearBottom)$('bottomButton').classList.add('hidden')});$('bottomButton').addEventListener('click',()=>{$('messages').scrollTop=$('messages').scrollHeight;nearBottom=true;$('bottomButton').classList.add('hidden')});document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible'&&current){void refreshHistory();if(!ws)connect()}});
(async()=>{prefillDeviceName();try{await load();showApp()}catch{showPair()}})();
</script></body></html>`
}
